services:
  - type: web
    name: zynochat-advanced
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
